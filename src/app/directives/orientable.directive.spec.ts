import { OrientableDirective } from './orientable.directive';

describe('OrientableDirective', () => {
  it('should create an instance', () => {
    const directive = new OrientableDirective();
    expect(directive).toBeTruthy();
  });
});
