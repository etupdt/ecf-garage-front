import { IsEmployeDirective } from './is-employe.directive';

describe('IsEmployeDirective', () => {
  it('should create an instance', () => {
    const directive = new IsEmployeDirective();
    expect(directive).toBeTruthy();
  });
});
